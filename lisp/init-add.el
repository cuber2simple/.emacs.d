;;; init.el --- Load the full configuration -*- lexical-binding: t -*-
;;; Commentary:

;; This file bootstraps the configuration, which is divided into
;; a number of other files.

;;; Code:


(use-package which-key
  :ensure t
  :init (which-key-mode))

(provide 'init-add)
;;; init-add.el ends here
